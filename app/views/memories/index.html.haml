-if @search_total
	%h3
		Found
		=@search_total
		results.

%h1 Listing memories

%table
	%tr
		%th
		%th
		%th

	- @memories.each do |memory|
		%tr
			%td=link_to memory.name, memory
			%td=memory.item

=paginate @search ? @search : @memories